#include<bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef vector<ll> vll;
typedef pair<int,int> pii;

int main()
{   
    int n,m;
    cin>>n>>m;
    vi actual(n);
    vi maximum(m);
    for(int i = 0;i<n;i++){
        cin>>actual[i];
    }
    for(int i = 0;i<m;i++){
        cin>>maximum[i];
    }
    
    
    return 0;
}